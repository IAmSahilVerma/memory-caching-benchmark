# memory-caching-benchmark
# Python 3.10+

# Core ML
torch>=2.2.0
torchvision>=0.17.0

# Mamba (state-space model)
# Install separately if on CUDA: pip install mamba-ssm causal-conv1d
# Fallback pure-PyTorch implementation included in models/mamba_mc.py
# mamba-ssm>=1.2.0

# Data
datasets>=2.18.0          # HuggingFace datasets (WikiText-2)
tokenizers>=0.15.0
numpy>=1.26.0

# Training utilities
tqdm>=4.66.0
einops>=0.7.0             # tensor rearrangement, used in MC attention over cached states

# Evaluation
torchmetrics>=1.3.0       # perplexity, accuracy helpers

# Experiment tracking
wandb>=0.16.0             # optional — comment out if not using

# Results & reporting
pandas>=2.2.0             # aggregating eval results to JSON/CSV